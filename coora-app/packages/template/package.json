{
	"name": "@coora/template",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"start": "onchange -i \"src/**/*\" -- npm run build",
		"build": "asc --config ./config/asconfig.json",
		"build:release": "npm run build -- --target release"
	}
}